# Concept of delta time
// TODO

# Inputs
a/d : move left/right from (0 to 6.26)rad varying the `PlayerAngle` variable.
w/s : this need to change the `playerX` and `playerY` variable aq to `PlayerAngle`

# Outer Looping

- we loop from x(0 to 640)
- think of 640 pencil standing from left to right 
- the middle one got x(320) `PlayerAngle` but what about x(0...319,320,321...640)
- `angle = (PlayerAngle - HalfFov) + (x/Width) * FOV`
    - this line of code give angle for every x (0 to 640)

# Inner Looping
```
for MAP[i32(PY + math.sin(angle) * dist) * MAP_W + i32(PX + math.cos(angle) * dist)] == 0 {
    dist += STEP_SIZE
} 
```
- so the inner loop increases from player position to the angle for its x with its STEP_SIZE
- and at the end we * MAP_W to turn it into map cordinate and check its a empty 
- and increases the dist += STEP_SIZE

# hight from the player distance to the wall
- if the player if close to wall its height is big
- if the player if far  to wall its height is small

* dist = 1.0
    - h = (HEIGHT / 1.0)
    - h = 480 : height of the wall
 
* dist = 4.0
    - h = (HEIGHT / 4.0)
    - h = 120 : height of the wall

* dist = 0.1 (inside the wall)
    - h = (HEIGHT / 0.1)
    - h = 480000 : height of the wall
    - clamp it : `if dist < HEIGHT do h = HEIGHT

# DrawLine
```
DrawLine(x, HALF_HEIGHT - h/2, x, HALF_HEIGHT + h/2, rl.RED)
```

- so the x is comming form the outer loop currently the far end vertical strip x = 0;
- and -h/2, and +h/2 is what making it look 3d
